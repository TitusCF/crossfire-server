FILES = items-extract.pl monster-extract.pl

all::

install::

depend::

archive::
	@if [ -d $(ARCHIVE)/doc/scripts ]; then set +x; \
	else (set -x; $(MKDIR) $(ARCHIVE)/doc/scripts); fi
	$(CP) $(FILES) $(ARCHIVE)/doc/scripts

patchlist::
	echo $(FILES) > .patchlist
